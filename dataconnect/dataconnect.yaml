specVersion: "v1beta"
serviceId: "kolaycms"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "kolaycms-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
